# ==============================================================================
# .dockerignore - Excludes files from Docker build context
# ==============================================================================
# This file works like .gitignore but for Docker builds. It tells Docker which
# files and directories to exclude when copying files to the container during
# the build process (COPY . /var/www/html in Dockerfile).
#
# Benefits:
# 1. Faster builds - less data to transfer to Docker daemon
# 2. Smaller images - excludes unnecessary files from final image
# 3. Security - prevents sensitive files from being copied into image
# 4. Consistency - regenerate dependencies inside container for correct environment
# ==============================================================================

# ==============================================================================
# Dependencies (Generated Inside Container)
# ==============================================================================
# Ignore vendor directory (will be generated in container)
# Vendor contains PHP dependencies installed by Composer
# We regenerate these inside the container to ensure compatibility with
# the container's PHP version and extensions (not your local machine's)
vendor/

# Ignore node_modules (JavaScript dependencies)
# These are installed by npm/yarn and can be huge (100+ MB)
# Should be regenerated inside container if needed for asset compilation
node_modules/

# ==============================================================================
# Environment & Configuration Files
# ==============================================================================
# Ignore environment backup files
# These may contain sensitive credentials and are not needed in container
.env.backup
.env.production

# ==============================================================================
# Logs & Cache
# ==============================================================================
# Ignore logs directory
# Logs are generated at runtime and shouldn't be copied from local machine
# Fresh logs will be created when the container runs
storage/logs/*

# ==============================================================================
# Version Control
# ==============================================================================
# Ignore git directory and configuration
# .git directory can be very large and is not needed for running the application
# Version control is only needed during development, not in production images
.git/
.gitignore

# ==============================================================================
# IDE & Editor Files
# ==============================================================================
# Ignore IDE configuration directories
# These are personal development environment settings
# Different developers may use different IDEs (PhpStorm, VS Code, etc.)
.idea/       # JetBrains IDEs (PhpStorm, IntelliJ)
.vscode/     # Visual Studio Code

# Vim swap files (created when editing with Vim)
*.swp        # Swap file (while editing)
*.swo        # Older swap file

# ==============================================================================
# Operating System Files
# ==============================================================================
# Ignore OS-specific files
# These are automatically generated by operating systems
.DS_Store    # macOS folder metadata
Thumbs.db    # Windows thumbnail cache

# ==============================================================================
# Documentation
# ==============================================================================
# Ignore documentation files
# README and docs are for developers, not needed to run the application
# Reduces image size without affecting functionality
README.md
docs/
